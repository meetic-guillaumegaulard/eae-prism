{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend API using Elysia",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "build": "bun build src/index.ts --outdir=dist --target=bun"
  },
  "dependencies": {
    "elysia": "^1.0.0"
  },
  "devDependencies": {
    "bun-types": "^1.0.0"
  }
}

