name: client
description: Multi-brand Flutter application
version: 1.0.0
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  design_system:
    path: ../../packages/design_system
  go_router: ^17.0.0
  # Voice search dependencies
  speech_to_text: ^7.0.0
  flutter_riverpod: ^2.5.1
  flutter_animate: ^4.5.0
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
